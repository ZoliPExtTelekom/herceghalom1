# Client (Browser)

Planned:
- Single-page HTML + Canvas rendering
- Connects to Python server WebSocket at `/ws`

Files:
- `index.html`: page + canvas
- `main.js`: client entrypoint (skeleton)
- `style.css`: minimal styling

